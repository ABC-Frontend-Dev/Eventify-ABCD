import { CopyButton } from "@/components/ui/copy-button";

export default function CopyButtonDemo() {
    return (
        <div className="flex items-center justify-center min-h-[400px] p-8">
            <CopyButton>Hello, World!</CopyButton>
        </div>
    );
}
